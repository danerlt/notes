# 微调数据集准备

## 数据质量的重要性

在大模型微调中，有一条被反复验证的黄金准则：**数据质量远比数据数量重要**。

Meta 在 LLaMA 2 的论文中指出，使用 27540 条精心筛选的高质量对话数据，其效果超过了数百万条自动生成的低质量数据。OpenAI 的 InstructGPT 同样只使用了约 13000 条人工标注数据就达到了显著的效果提升。

### 数据质量的维度

| 维度 | 描述 | 检查方法 |
|------|------|---------|
| **准确性** | 答案是否正确、无事实错误 | 专家审核、交叉验证 |
| **完整性** | 回答是否充分、覆盖问题所有方面 | 人工评测 |
| **格式一致性** | 数据格式是否统一 | 正则检查、schema 验证 |
| **多样性** | 覆盖不同主题、风格、难度 | 聚类分析 |
| **无毒性** | 不含有害、歧视性内容 | 内容过滤模型 |

---

## 数据格式

### Alpaca 格式

由 Stanford Alpaca 项目提出，是最早被广泛采用的指令微调格式。

```json
{
    "instruction": "将以下文本翻译成英文",
    "input": "今天天气真好，适合出去散步。",
    "output": "The weather is really nice today, perfect for a walk."
}
```

- `instruction`：任务描述（必填）
- `input`：任务的具体输入内容（可选，无输入时留空 `""`）
- `output`：期望的输出（必填）

**优点：** 简单直观，适合单轮指令任务

**缺点：** 不支持多轮对话，无法区分系统提示

### ChatML 格式

OpenAI 推广的多轮对话格式，被 Qwen、Mistral 等模型广泛使用。

```
<|im_start|>system
你是一位专业的Python编程助手，请用简洁的代码解决问题。
<|im_end|>
<|im_start|>user
如何用Python读取一个CSV文件？
<|im_end|>
<|im_start|>assistant
可以使用 pandas 库来读取 CSV 文件：

```python
import pandas as pd
df = pd.read_csv('data.csv')
print(df.head())
```
<|im_end|>
```

对应的 JSON 存储格式：

```json
{
    "messages": [
        {
            "role": "system",
            "content": "你是一位专业的Python编程助手，请用简洁的代码解决问题。"
        },
        {
            "role": "user",
            "content": "如何用Python读取一个CSV文件？"
        },
        {
            "role": "assistant",
            "content": "可以使用 pandas 库来读取 CSV 文件：\n\n```python\nimport pandas as pd\ndf = pd.read_csv('data.csv')\nprint(df.head())\n```"
        }
    ]
}
```

### ShareGPT 格式

从 ShareGPT 网站爬取的真实用户对话格式，支持多轮对话。

```json
{
    "id": "sample_001",
    "conversations": [
        {
            "from": "human",
            "value": "什么是机器学习？"
        },
        {
            "from": "gpt",
            "value": "机器学习是人工智能的一个子领域，它使计算机系统能够通过数据自动学习和改进，而无需进行明确的编程。"
        },
        {
            "from": "human",
            "value": "能给我一个具体的例子吗？"
        },
        {
            "from": "gpt",
            "value": "当然！垃圾邮件过滤器就是一个典型例子。系统通过分析大量已标注的邮件（垃圾/非垃圾），学习识别垃圾邮件的特征，之后就能自动判断新邮件是否是垃圾邮件。"
        }
    ]
}
```

### 格式选择建议

| 场景 | 推荐格式 |
|------|----------|
| 单轮指令任务（翻译、分类、提取） | Alpaca |
| 多轮对话助手 | ChatML 或 ShareGPT |
| 使用 LLaMA-Factory | ShareGPT（内置支持最好） |
| 使用 TRL / Unsloth | ChatML（messages 格式） |

---

## 数据收集方法

### 方法一：人工标注

质量最高，成本最高。适合核心任务的种子数据集。

```python
# 人工标注数据的简单管理脚本
import json
from pathlib import Path
from datetime import datetime

class AnnotationManager:
    def __init__(self, save_path: str):
        self.save_path = Path(save_path)
        self.data = []
        # 如果文件存在则加载已有数据
        if self.save_path.exists():
            with open(self.save_path, "r", encoding="utf-8") as f:
                self.data = json.load(f)

    def add_sample(self, instruction: str, output: str, input_text: str = "") -> None:
        """添加一条标注样本"""
        sample = {
            "instruction": instruction,
            "input": input_text,
            "output": output,
            "annotated_at": datetime.now().isoformat(),
        }
        self.data.append(sample)
        self._save()
        print(f"已添加第 {len(self.data)} 条样本")

    def _save(self) -> None:
        with open(self.save_path, "w", encoding="utf-8") as f:
            json.dump(self.data, f, ensure_ascii=False, indent=2)

# 使用示例
manager = AnnotationManager("annotations.json")
manager.add_sample(
    instruction="分析以下合同条款中的潜在法律风险",
    input_text="甲方应在签约后30日内支付全部款项...",
    output="该条款存在以下风险：1. 未明确违约责任...",
)
```

### 方法二：LLM 生成（Self-Instruct）

利用强大的 LLM（GPT-4、Claude）生成训练数据，再进行过滤。

```python
from openai import OpenAI
import json
import time

client = OpenAI()

def generate_instruction_data(
    seed_tasks: list[str],
    num_samples: int = 100,
    domain: str = "通用",
) -> list[dict]:
    """
    使用 GPT-4 生成指令微调数据

    Args:
        seed_tasks: 种子任务列表，用于指导生成方向
        num_samples: 目标生成数量
        domain: 数据所属领域

    Returns:
        生成的数据列表（Alpaca 格式）
    """
    generated_data = []
    seed_examples = "\n".join([f"- {task}" for task in seed_tasks[:5]])

    prompt_template = f"""
你是一个数据生成助手。请生成5条{domain}领域的指令微调数据。

参考以下任务类型：
{seed_examples}

要求：
1. 指令要多样，涵盖不同类型的任务
2. 答案要准确、完整
3. 难度适中

请严格按照以下 JSON 数组格式输出，不要有其他内容：
[
  {{
    "instruction": "...",
    "input": "...",
    "output": "..."
  }}
]
"""

    batch_size = 5
    for i in range(0, num_samples, batch_size):
        try:
            response = client.chat.completions.create(
                model="gpt-4o-mini",
                messages=[{"role": "user", "content": prompt_template}],
                temperature=0.8,  # 较高温度增加多样性
            )
            batch = json.loads(response.choices[0].message.content)
            generated_data.extend(batch)
            print(f"已生成 {len(generated_data)} 条数据...")
            time.sleep(1)  # 避免触发速率限制
        except (json.JSONDecodeError, Exception) as e:
            print(f"第 {i} 批次生成失败: {e}")
            continue

    return generated_data[:num_samples]


# 使用示例
seed_tasks = [
    "将文本翻译成英文",
    "总结文章的主要观点",
    "回答关于Python编程的问题",
    "纠正句子中的语法错误",
]
data = generate_instruction_data(seed_tasks, num_samples=50, domain="教育")
```

### 方法三：开源数据集

直接使用或基于开源数据集进行加工，成本最低。

```python
from datasets import load_dataset

# 加载 Alpaca 中文数据集
dataset = load_dataset("shibing624/alpaca-zh", split="train")
print(f"数据集大小: {len(dataset)}")
print(f"字段: {dataset.column_names}")
print(f"示例:\n{dataset[0]}")

# 加载 ShareGPT 格式的中文对话数据集
dataset_chat = load_dataset("shareAI/shareGPT4-zh", split="train")
```

---

## 数据清洗与过滤

高质量的数据清洗通常比增加数据量更有效。

```python
import re
from typing import Optional

def clean_sample(sample: dict) -> Optional[dict]:
    """
    清洗单条 Alpaca 格式数据，返回清洗后的数据，不合格则返回 None
    """
    instruction = sample.get("instruction", "").strip()
    output = sample.get("output", "").strip()
    input_text = sample.get("input", "").strip()

    # 1. 过滤空数据
    if not instruction or not output:
        return None

    # 2. 过滤过短的指令或输出（可能是无意义数据）
    if len(instruction) < 5 or len(output) < 10:
        return None

    # 3. 过滤过长的数据（超出模型上下文长度）
    total_length = len(instruction) + len(input_text) + len(output)
    if total_length > 4000:  # 按字符数粗略过滤
        return None

    # 4. 过滤包含特殊标记的数据（如模型输出的 token 标记）
    forbidden_patterns = [
        r"<\|im_start\|>",
        r"<\|im_end\|>",
        r"\[INST\]",
        r"###\s*Human:",
        r"###\s*Assistant:",
    ]
    combined_text = instruction + output
    for pattern in forbidden_patterns:
        if re.search(pattern, combined_text):
            return None

    # 5. 过滤重复内容（指令和输出高度相似）
    if instruction in output or output in instruction:
        return None

    # 6. 过滤含有明显乱码的数据（非中文非英文比例过高）
    def is_garbled(text: str, threshold: float = 0.3) -> bool:
        valid_chars = sum(
            1 for c in text
            if "\u4e00" <= c <= "\u9fff"  # 中文
            or c.isalpha()               # 英文字母
            or c.isdigit()               # 数字
            or c in "，。！？、；：""''（）【】\n\t .,!?;:\"'()[]"
        )
        return valid_chars / max(len(text), 1) < (1 - threshold)

    if is_garbled(output):
        return None

    return {
        "instruction": instruction,
        "input": input_text,
        "output": output,
    }


def filter_dataset(data: list[dict]) -> list[dict]:
    """批量清洗数据集"""
    cleaned = []
    skipped = 0
    for sample in data:
        result = clean_sample(sample)
        if result:
            cleaned.append(result)
        else:
            skipped += 1

    print(f"原始数据: {len(data)} 条")
    print(f"清洗后: {len(cleaned)} 条")
    print(f"过滤比例: {skipped / len(data) * 100:.1f}%")
    return cleaned
```

### 去重处理

```python
from hashlib import md5

def deduplicate_dataset(data: list[dict], similarity_threshold: float = 0.9) -> list[dict]:
    """
    基于精确哈希的数据去重
    对于模糊去重可使用 MinHash 或 SimHash 算法
    """
    seen_hashes = set()
    deduplicated = []

    for sample in data:
        # 使用 instruction + input 作为去重键
        key = sample["instruction"] + sample.get("input", "")
        key_hash = md5(key.encode("utf-8")).hexdigest()

        if key_hash not in seen_hashes:
            seen_hashes.add(key_hash)
            deduplicated.append(sample)

    removed = len(data) - len(deduplicated)
    print(f"去重: 移除 {removed} 条重复数据，剩余 {len(deduplicated)} 条")
    return deduplicated
```

---

## 数据增强技术

### 指令改写

对同一任务用不同方式描述指令，增加多样性。

```python
def augment_by_instruction_rewrite(
    sample: dict,
    rewrite_fn,  # 接收一个改写函数（可调用 LLM）
    num_rewrites: int = 3,
) -> list[dict]:
    """
    通过改写 instruction 来增强数据
    相同的 input 和 output，不同的指令表达方式
    """
    augmented = [sample]  # 保留原始数据
    original_instruction = sample["instruction"]

    for _ in range(num_rewrites):
        new_instruction = rewrite_fn(original_instruction)
        if new_instruction and new_instruction != original_instruction:
            augmented.append({
                **sample,
                "instruction": new_instruction,
            })

    return augmented


# 简单的规则改写示例（不依赖 LLM）
INSTRUCTION_PREFIXES = [
    "请", "帮我", "你能", "能否", "我需要你",
    "麻烦", "劳驾", "请帮我",
]

def simple_rewrite(instruction: str) -> str:
    """在指令前添加不同的前缀来简单增强"""
    import random
    prefix = random.choice(INSTRUCTION_PREFIXES)
    # 去掉原有前缀后重新添加
    clean = instruction.lstrip("请帮我你能否需要麻烦劳驾")
    return f"{prefix}{clean}"
```

### 回答多样化

对同一问题生成多种正确答案（可用于偏好学习数据）。

```python
def create_preference_data(
    instruction: str,
    input_text: str,
    chosen_output: str,    # 好的回答
    rejected_output: str,  # 差的回答
) -> dict:
    """
    构建 DPO/RLHF 偏好学习数据格式
    """
    return {
        "instruction": instruction,
        "input": input_text,
        "chosen": chosen_output,
        "rejected": rejected_output,
    }
```

---

## 常用开源数据集推荐

### 中文数据集

| 数据集 | 规模 | 类型 | 链接 |
|--------|------|------|------|
| alpaca-zh | 5.2 万条 | 指令跟随 | shibing624/alpaca-zh |
| BELLE-data | 100 万条 | 指令跟随 | BelleGroup/train_1M_CN |
| firefly-train | 115 万条 | 多任务指令 | YeungNLP/firefly-train-1.1M |
| COIG-CQIA | 4.4 万条 | 高质量中文问答 | m-a-p/COIG-CQIA |
| shareGPT4-zh | 6 万条 | 多轮对话 | shareAI/shareGPT4-zh |

### 英文数据集

| 数据集 | 规模 | 类型 | 说明 |
|--------|------|------|------|
| OpenAssistant | 16 万条 | 多轮对话 | 多语言，高质量人工标注 |
| Dolly-15k | 1.5 万条 | 指令跟随 | Databricks 发布，商用友好 |
| FLAN | 数百万条 | 多任务 | Google 发布，任务类型多样 |
| WizardLM-Evol | 70 万条 | 复杂指令 | 通过 Evol-Instruct 方法生成 |

---

## 数据集划分

```python
import json
import random
from pathlib import Path

def split_dataset(
    data: list[dict],
    train_ratio: float = 0.9,
    val_ratio: float = 0.05,
    test_ratio: float = 0.05,
    seed: int = 42,
    output_dir: str = "./data",
) -> dict[str, list]:
    """
    将数据集划分为训练集、验证集、测试集

    Args:
        data: 完整数据列表
        train_ratio: 训练集比例（默认 90%）
        val_ratio: 验证集比例（默认 5%）
        test_ratio: 测试集比例（默认 5%）
        seed: 随机种子，确保可复现
        output_dir: 输出目录

    Returns:
        包含 train/val/test 键的字典
    """
    assert abs(train_ratio + val_ratio + test_ratio - 1.0) < 1e-6, "比例之和必须为 1"

    random.seed(seed)
    shuffled = data.copy()
    random.shuffle(shuffled)

    n = len(shuffled)
    n_train = int(n * train_ratio)
    n_val = int(n * val_ratio)

    splits = {
        "train": shuffled[:n_train],
        "val": shuffled[n_train : n_train + n_val],
        "test": shuffled[n_train + n_val :],
    }

    # 保存到文件
    output_path = Path(output_dir)
    output_path.mkdir(parents=True, exist_ok=True)

    for split_name, split_data in splits.items():
        file_path = output_path / f"{split_name}.json"
        with open(file_path, "w", encoding="utf-8") as f:
            json.dump(split_data, f, ensure_ascii=False, indent=2)
        print(f"{split_name}: {len(split_data)} 条 -> {file_path}")

    return splits
```

---

## 完整示例：构建 Instruction Tuning 数据集

以下示例展示从原始数据到可直接用于训练的完整流程。

```python
import json
import random
from pathlib import Path
from datasets import load_dataset, Dataset

def build_instruction_dataset(
    source: str = "shibing624/alpaca-zh",
    max_samples: int = 5000,
    output_dir: str = "./my_dataset",
    seed: int = 42,
) -> None:
    """
    构建一个可直接用于 SFT 训练的指令微调数据集
    包含：加载 -> 清洗 -> 去重 -> 格式转换 -> 划分 -> 保存
    """
    print("=" * 50)
    print("步骤 1: 加载原始数据集")
    print("=" * 50)
    raw_dataset = load_dataset(source, split="train")
    raw_data = list(raw_dataset)
    print(f"原始数据量: {len(raw_data)} 条")

    print("\n步骤 2: 数据清洗")
    print("=" * 50)
    cleaned_data = []
    for sample in raw_data:
        result = clean_sample(sample)  # 使用前面定义的 clean_sample 函数
        if result:
            cleaned_data.append(result)
    print(f"清洗后数据量: {len(cleaned_data)} 条")

    print("\n步骤 3: 去重")
    print("=" * 50)
    deduped_data = deduplicate_dataset(cleaned_data)  # 使用前面定义的去重函数

    print("\n步骤 4: 随机采样")
    print("=" * 50)
    random.seed(seed)
    if len(deduped_data) > max_samples:
        final_data = random.sample(deduped_data, max_samples)
    else:
        final_data = deduped_data
    print(f"最终数据量: {len(final_data)} 条")

    print("\n步骤 5: 转换为 ChatML 格式")
    print("=" * 50)

    def alpaca_to_chatml(sample: dict) -> dict:
        """将 Alpaca 格式转换为 ChatML messages 格式"""
        user_content = sample["instruction"]
        if sample.get("input"):
            user_content += f"\n\n{sample['input']}"
        return {
            "messages": [
                {"role": "user", "content": user_content},
                {"role": "assistant", "content": sample["output"]},
            ]
        }

    chatml_data = [alpaca_to_chatml(s) for s in final_data]

    print("\n步骤 6: 统计数据特征")
    print("=" * 50)
    lengths = []
    for sample in final_data:
        total = (
            len(sample["instruction"])
            + len(sample.get("input", ""))
            + len(sample["output"])
        )
        lengths.append(total)

    print(f"平均长度: {sum(lengths) / len(lengths):.0f} 字符")
    print(f"最大长度: {max(lengths)} 字符")
    print(f"最小长度: {min(lengths)} 字符")
    # 90th percentile
    lengths_sorted = sorted(lengths)
    p90_idx = int(len(lengths_sorted) * 0.9)
    print(f"P90 长度: {lengths_sorted[p90_idx]} 字符")

    print("\n步骤 7: 划分并保存")
    print("=" * 50)
    split_dataset(chatml_data, output_dir=output_dir)  # 使用前面定义的划分函数

    print("\n数据集构建完成！")
    print(f"输出目录: {Path(output_dir).absolute()}")


# 执行完整流程
if __name__ == "__main__":
    build_instruction_dataset(
        source="shibing624/alpaca-zh",
        max_samples=5000,
        output_dir="./my_sft_dataset",
    )
```

---

## 数据准备最佳实践

1. **先小后大**：先用 100-200 条数据跑通完整训练流程，验证代码和格式无误后再扩大数据量
2. **标注一致性**：多人标注时要制定明确的标注规范，定期抽查和对齐标注标准
3. **保留原始数据**：清洗和转换操作在副本上进行，永远保留原始数据用于追溯
4. **验证集要有代表性**：验证集应覆盖所有任务类型，不能只包含容易的样本
5. **记录数据来源**：每条数据标注来源（人工/LLM生成/开源），便于后续质量追溯
6. **定期审查低质样本**：训练过程中 loss 异常高的样本往往是噪声数据，值得人工复查
